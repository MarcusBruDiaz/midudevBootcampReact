
//{title.map((c,i)=> console.log(c))}
//{title.name}
/*
<>
<h1>
  
    

</h1>
</>*/

/*const Total = ({parts})=>{

  const total = parts.reduce((acc, cv)=>{
    return acc + cv.exercises 
  },0);



 let suma=0;
  for(let i=0; i<parts.length;i++){
    suma+=parts[i].exercises    total a lo antiguo mas vervose
  }

  return(
    <div>
      <p>Total of {total} course</p>
    </div>
  )
}*/





/*<Content content={course}/>
<Total parts={course} />
<Header title={course[1]}/>*/

/*{course.map((c,i)=>{
  console.log(c)
  return <div key={c.id}>
    <Header key={c.id}  title={c}/>
  </div>
})}*/









 /*let suma=0;
  for(let i=0; i<parts.length;i++){
    suma+=parts[i].exercises    total a lo antiguo mas vervose
  }   mi forma clasica*/ 







/*const Content = (props) =>{
  const arr = props.content
  console.log(arr)

  return(
    <div>
      {arr.map((c,i)=>(       
        <Part key={c.id} content={c}/>
      ))}
    </div>
  )
}*/  //FORMA QUE FUCIONA CON LA ANTIGUA ESTRUCTURA DE DATOS

